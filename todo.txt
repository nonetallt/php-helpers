# Validation
* refactor
* null rule

# Describe
* Describe string, describesCharacters method that lists all characters example: a, space, tab
* Describe difference, string difference can be useful for debugging tests

# HTTP
* status codes
    * use status code for request error when applicable (so that the error can be used to know wether user should attempt to retry request)
* base url parser

# SetsAttributesTrait
* define validation / processing rules
* "array|size:2|->serialize"
* define __set, use parent __set after processing

# Collection
* toArray(): check if item class has "toArray()" (and check that return type is array) and map all items by default if true
    * toArray(bool recursive = false) recursively array items if options is true
